<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppMobile.EditarTarefa"
             Title="">
    <ScrollView>
    <VerticalStackLayout>
        <Label 
             Text="Detalhes da Tarefa:"
             FontSize="28" 
             FontAttributes="Bold"
             HorizontalOptions="Center"/>


            <Border StrokeShape="RoundRectangle 15" 
                    Padding="15" 
                    BackgroundColor="{StaticResource Gray100}">
                <VerticalStackLayout Spacing="5">
                    <Label Text="Nome atual:" FontAttributes="Bold" FontSize="12" TextColor="Gray"/>
                    <Label Text="{Binding Nome}" FontSize="18"/>

                    <Label Text="Descrição atual:" FontAttributes="Bold" FontSize="12" TextColor="Gray" Margin="0,10,0,0"/>
                    <Label Text="{Binding Descrição}" FontSize="16"/>
                </VerticalStackLayout>
            </Border>

            <Label Text="Edição" FontAttributes="Bold" FontSize="18" Margin="0,10,0,0"/>

            <Entry Placeholder="Novo nome..." ClearButtonVisibility="WhileEditing" x:Name="txtNome"/>
            <Entry Placeholder="Nova descrição..." ClearButtonVisibility="WhileEditing" x:Name="txtDescricao"/>

            <Border StrokeShape="RoundRectangle 10" Padding="10" BackgroundColor="#F0F0F0">
                <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                    <Label Text="Status:" VerticalOptions="Center"/>
                    <Label x:Name="lblStatus" FontAttributes="Bold" VerticalOptions="Center"/>
                    <Button Text="Alterar" Clicked="Alterar_Clicked" WidthRequest="100"/>
                </HorizontalStackLayout>
            </Border>

        

        <Button Text="Salvar" WidthRequest="250" Margin="0,50,0,50" Clicked="Salvar_Clicked"/>
       <Label Text="SubTarefas" 
                   FontSize="22" 
                   FontAttributes="Bold"
                   Margin="0,20,0,0"/>

        
        
        
        
        
            <CollectionView x:Name="colSubTasks" ItemsSource="{Binding SubTasks}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeShape="RoundRectangle 10" 
                                Padding="10" 
                                Margin="0,5" 
                                BackgroundColor="White">
                            <StackLayout>
                                <Label Text="{Binding Nome}" VerticalOptions="Center"/>
                                <ImageButton  HorizontalOptions="EndAndExpand" WidthRequest="10" HeightRequest="5" Source="edit.png" Margin=" 0,0,0,0"/>
                            </StackLayout>
                           
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="Adicionar Subtarefa" 
                    WidthRequest="250" 
                    Margin="0,20,0,20" 
                    BackgroundColor="Transparent" 
                    TextColor="{StaticResource Primary}"
                    BorderColor="{StaticResource Primary}"
                    BorderWidth="1"/>


    </VerticalStackLayout>
    </ScrollView>
</ContentPage>